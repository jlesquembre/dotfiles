

conky.config = {
    cpu_avg_samples = 2,
    net_avg_samples = 2,
    no_buffers = true,
    out_to_stderr = false,
    uppercase = false,
    use_spacer = 'none',
    short_units = true,
    if_up_strictness = 'address',

    override_utf8_locale = false,
    total_run_times = 0,

    out_to_x = false,

    own_window = false,

    out_to_console = true,

    -- Set to yes if you want Conky to be forked in the background
    background = false,

    -- Update interval in seconds
    update_interval = 5.0,

    lua_load = '~/dotfiles/i3/conky.lua',
    lua_draw_hook_pre = 'main',

};


conky.text = [[ ]];
